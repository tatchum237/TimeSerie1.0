# Time serie



- I kept the hyperparameter (1,0,3) because it gives better performance of the model
- MAE gives me a value of 183 for the prophet model and 180 for the ARIMA model
- To improve performance, I need to train other models and improve performance

to run the model: streamlit run main.py